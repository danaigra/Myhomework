<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dana.myhomework">

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="index"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".homework1_1" />
        <activity android:name=".homework1_2" />
        <activity android:name=".ConstraintLayout1" />
        <activity android:name=".ConstraintLayout2" />
        <activity android:name=".ConstraintLayout3" />
        <activity android:name=".MyBirthdays" />
        <activity android:name=".settings" />
    </application>

</manifest>